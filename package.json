{
  "name": "core-assistant",
  "description": "Çok amaçlı sekme ve odaklanma asistanı tarayıcı eklentisi",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build && node scripts/postbuild.mjs",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.6",
    "sharp": "^0.35.3",
    "typescript": "^7.0.2",
    "wxt": "^0.21.4"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0"
  }
}
